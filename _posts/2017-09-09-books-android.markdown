---
layout: post
title:  "《第一行代码 Android》读后感"
date:   2017-09-09 22:46:30 +0800
categories: iBooks
---
《第一行代码：Android》系统全面、循序渐进地介绍了 Android 软件开发的知识、经验和技巧。作为 Android 开发入门级选手，通过通读全书内容并配以书中的实践，我对 Android 开发有了一个初步整体认知框架。

本书共分为 15 章节，由浅入深地介绍了 Android 发展历史及开发工具，四大组件，UI 控件，数据存储，网络请求，Material Design 等知识，覆盖了从搭建项目到发布应用市场的整个开发流程。

以下是一些读书总结。

### 开发环境

[Android Studio](https://developer.android.google.cn/studio/index.html) 作为官方 Android IDE，提供用于为各类 Android 设备高效开发的工具。

> 利用世界一流的代码编辑、调试、性能测试工具、一套灵活的构建体系以及一套即时构建／部署系统，您可以专注于构建独特的高品质应用。

你可以通过官网进行下载安装。在安装和配置过程中需要使用相关外网去访问 Google 相关网站。

### 认识活动

活动（ Activity ）是最容易吸引用户的地方，它是一种可以包含用户界面的组件，主要用于和用户进行交互。如果把应用程序类比为一幢房子的话，活动就是其中的屋子。每间屋子有着自己的装修风格和存储物件，可以给每间屋子进行定义功能场景，例如主卧、客厅、厨房、卫生间等，通过钥匙（Intent）可以在不同的屋子间进行穿梭。

#### 活动的启动模式

活动的启动模式总共有 4 种，分别是 standard、singleTop、singleTask 和 singleInstance，可以在 AndroidManifest.xml 中通过给 activity 标签指定 **android:launchMode** 属性来选择启动模式。

其中 4 种启动模式各自的应用场景如下：

1. standard: 对于使用 standard 模式的活动，系统不会在乎这个活动是否已经在返回栈中存在，每次启动都会创建该活动的一个新的实例。
2. singleTop：当活动的启动模式指定为 singleTop，在启动活动时如果发现返回栈的栈顶已经是该活动，则认为可以直接使用，而不需要再创建新的活动实例。
3. singleTask：当活动的启动模式指定为 singleTask，每次启动该活动时系统首先会在返回栈中检查是否存在该活动的实例，如果发现已经存在则直接使用该实例，并把在这个活动之上的所有活动统统出栈，如果没有发现就会创建一个新的活动实例。
4. singleInstance：指定为 singleInstance 模式的活动会启用一个新的返回栈来管理这个活动。

### UI 开发

Android 中的界面开发基于 XML 语言，熟悉掌握 XML 语言和相应布局、控件后就能编写出设计复杂优美的界面效果。布局和控件就是每间房屋中的框架结构和物件，例如在客厅一般都会安装电视机柜，然后把电视机以及机顶盒放到柜子的中央，再在柜子周边依次摆放一些装饰小物件，以达到赏心悦目的效果。

### 广播

Android 程序中可以注册自己感兴趣的广播，这样程序就只会接受到自己所关心的广播内容，这些广播可能来自于系统的，也可能是来自于其它应用程序的。Android 中的广播主要可以分为两种类型：标准广播和有序广播。

- 标准广播是一种完全异步执行的广播，在广播发出之后，所有的广播接收器几乎都会在同一时间收到这条广播消息，因此它们之间没有任何的先后顺序。这种广播效率会比较高，但同时也意味着它是无法被截断的。
- 有序广播则是一种同步执行的广播，在广播发出之后，同一时刻只会有一个广播接收器能够收到这条广播消息，当这个广播接收器中的逻辑执行完毕后，广播才会继续传递。所以此时的广播接收器是有先后顺序的，优先级高的广播接收器就可以先收到广播消息，并且前面的广播接收器还可以截断正在传递的广播，这样后面的广播接收器就无法收到广播消息了。

### 内容提供器

内容提供器（ Content Provider）主要用于在不同的应用程序之间实现数据共享的功能，它提供了一套完整的机制，允许一个程序访问另一个程序中的数据，同时还能保证被访问数据的安全性。

不同于文件存储和 SharedPreferences 存储中的两种全局可读写操作模式，内容提供器可以选择只对哪一部份数据进行共享，从而保证我们程序中的隐私数据不会有泄漏的风险。
